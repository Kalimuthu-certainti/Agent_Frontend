{
  "name": "agent-control-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only dashboard over the dev-agent run log. File-backed today, Postgres behind the same seam later.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "build": "npm --prefix web run build",
    "dev": "npm --prefix web run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}
